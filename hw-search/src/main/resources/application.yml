# DataSource Config
server:
  port: 8888
spring:
  application:
    name: hw-search
  data:
    elasticsearch:
      cluster-name: kobe
      cluster-nodes: 127.0.0.1:9300
      repositories:
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@localhost:8761/eureka/